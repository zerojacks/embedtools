# 智能Excel任务定义提取工具

这是一个基于 Next.js 的 React 应用，用于智能识别和提取 Excel 文件中的任务定义。

## 功能特性

- 🔍 智能识别 Excel 结构
- 📊 按任务列提取完整定义（任务属性+测量点范围+任务号）
- 🔄 支持多工作表处理
- 🔍 搜索和筛选功能
- 📤 导出 JSON 和 INI 格式
- 🎨 现代化 UI 界面

## 安装和运行

### 使用 pnpm（推荐）

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

### 使用 npm

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 使用 yarn

```bash
# 安装依赖
yarn install

# 启动开发服务器
yarn dev
```

## 访问应用

启动成功后，在浏览器中访问：

- 主页（自动跳转）: http://localhost:3000
- 任务提取工具: http://localhost:3000/task-extract

## 项目结构

```
├── pages/
│   ├── index.tsx          # 主页（跳转页面）
│   ├── task-extract.tsx   # 任务提取工具主页面
│   └── _app.tsx          # Next.js 应用配置
├── styles/
│   └── globals.css       # 全局样式（Tailwind CSS）
├── package.json          # 项目依赖和脚本
├── next.config.js        # Next.js 配置
├── tailwind.config.js    # Tailwind CSS 配置
├── postcss.config.js     # PostCSS 配置
└── tsconfig.json         # TypeScript 配置
```

## 技术栈

- **框架**: Next.js 14
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **图标**: Lucide React
- **Excel 处理**: SheetJS (xlsx)
- **包管理**: pnpm

## 使用说明

1. 启动应用后访问 http://localhost:3000
2. 点击上传区域选择 Excel 文件（支持 .xlsx, .xls 格式）
3. 工具会自动识别 Excel 结构并提取任务定义
4. 可以按工作表筛选和搜索任务
5. 支持导出为 JSON 或 INI 格式

## 故障排除

如果遇到网络问题无法安装依赖，可以尝试：

1. 切换 npm 源：
   ```bash
   npm config set registry https://registry.npmjs.org/
   ```

2. 或使用淘宝源：
   ```bash
   npm config set registry https://registry.npmmirror.com/
   ```

3. 清除缓存：
   ```bash
   npm cache clean --force
   # 或
   pnpm store prune
   ```